// bouncing ball, from podelski's paper
//

.vars {
x, y, clk, t 
}

.pvars {
x', y', clk', t' 
}

.trans a1 : l1 -> l2 {
 t'>=0, clk'=clk-t', clk>=0,
 x>=y, y>=0,
 x'=x-t', y'=y-2*t',
 y<=0
}

.trans a2 : l2 -> l1 {
 t'>=0, clk'=clk-t', clk>=0,
 x>=y, y>=0,
 x'=x-t', y'=y+2*t',
 x<=y
}
