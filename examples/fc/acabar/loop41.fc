.vars {
  x, y, z, n
}

.pvars {
  x', y', z', n'
}

.trans t0 : n -> n {
  -1 * x + -1 * y <= 0,
  1 * x + -1 * n <= 0,
  -2 * x + -1 * y + 1 * x' = 0,
  -1 * z + 1 * y' = 0,
  -1 * z + 1 * z' = 1,
  -1 * n + 1 * n' = 0
}

