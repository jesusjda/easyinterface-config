.vars {
  x, y
}

.pvars {
  xp, yp
}

.trans t0 : n -> n {
  1 * x = 1,
  -1 * y <= 0,
  1 * x + 1 * xp = 1,
  1 * x + -1 * y + 1 * yp = 0
}
.trans t1 : n -> n {
  1 * x = 0,
  -1 * y <= 0,
  1 * x + 1 * xp = 1,
  1 * x + -1 * y + 1 * yp = 0
}

