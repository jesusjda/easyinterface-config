.vars {
  x, y, z
}

.pvars {
  x', y', z'
}

.trans t0 : n -> n {
  -1 * x <= 0,
  1 * y <= 10,
  1 * z <= 1,
  -1 * z <= 0,
  -1 * x + -1 * y + -1 * z + 1 * x' = -10,
  -1 * y + 1 * z + 1 * y' = 0,
  1 * z + 1 * z' = 1
}

